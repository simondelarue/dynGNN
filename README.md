# Node embedding for dynamic graphs

This repository contains research about embedding methods for time varying graphs.

`GNN/` is a GNN-based method on sequences of interaction, and `spectral` focuses on updating spectral embeddings over time.
